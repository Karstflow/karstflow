use serde::Deserialize;

#[derive(Debug, Default, Deserialize)]
pub struct IngressPolicyToml {
    pub schema_version: Option<u32>,
    pub ingress_mode: Option<String>,
    pub udp_bind_address: Option<String>,
    pub udp_quic_source_port: Option<u16>,
    pub udp_gossip_source_port: Option<u16>,
    pub udp_bundle_source_port: Option<u16>,
    pub udp_rpc_source_port: Option<u16>,
    pub udp_max_packets_per_tick: Option<u32>,
    pub max_payload_bytes: Option<usize>,
    pub allow_quic_source: Option<bool>,
    pub allow_gossip_source: Option<bool>,
    pub allow_bundle_source: Option<bool>,
    pub allow_rpc_source: Option<bool>,
    pub dedup_window_capacity: Option<usize>,
    pub quic_min_gap_ticks: Option<u64>,
    pub gossip_min_gap_ticks: Option<u64>,
    pub bundle_min_gap_ticks: Option<u64>,
    pub rpc_min_gap_ticks: Option<u64>,
    pub quic_burst_capacity: Option<u64>,
    pub gossip_burst_capacity: Option<u64>,
    pub bundle_burst_capacity: Option<u64>,
    pub rpc_burst_capacity: Option<u64>,
    pub quic_burst_refill_ticks: Option<u64>,
    pub gossip_burst_refill_ticks: Option<u64>,
    pub bundle_burst_refill_ticks: Option<u64>,
    pub rpc_burst_refill_ticks: Option<u64>,
    pub quic_cost_budget_per_window: Option<u64>,
    pub gossip_cost_budget_per_window: Option<u64>,
    pub bundle_cost_budget_per_window: Option<u64>,
    pub rpc_cost_budget_per_window: Option<u64>,
    pub quic_cost_budget_window_ticks: Option<u64>,
    pub gossip_cost_budget_window_ticks: Option<u64>,
    pub bundle_cost_budget_window_ticks: Option<u64>,
    pub rpc_cost_budget_window_ticks: Option<u64>,
    pub egress_retry_buffer_capacity: Option<usize>,
    pub egress_retry_max_wait_ticks: Option<u32>,
    pub synthetic_batch_size_per_tick: Option<u32>,
    pub synthetic_idle_ticks_between_batches: Option<u32>,
    pub synthetic_payload_bytes: Option<usize>,
    pub synthetic_source_weight_quic: Option<u32>,
    pub synthetic_source_weight_gossip: Option<u32>,
    pub synthetic_source_weight_bundle: Option<u32>,
    pub synthetic_source_weight_rpc: Option<u32>,
    pub tvu_bind_address: Option<String>,
}
